* fix the cproto noise
* review the executable output quality
* add indent
* drop the non-ANSI #includes
* use the real headers instead of test/include
* no more lib version, lib=src, mwplight only produces the executable
