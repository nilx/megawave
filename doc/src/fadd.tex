{\em fadd} adds pixel by pixel the two input images. 
These images must have the same size.
After the addition, a threshold or a normalization can be done
by selecting the options (see module \verb+fthre+).
