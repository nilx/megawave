This module computes the bounding box enclosing a set of Fcurves. The output
$xmin$, $xmax$, $ymin$ and $ymax$ are the minimum and maximum
values taken by the $x$ and $y$ coordinates over all points of all curves.
