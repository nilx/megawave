This module prints on standart output the content of a Fsignal.
The index of each element can appear (with \verb+-v+ option), and
a sub-interval of indices to be considered can be specified
({\it i1} and {\it i2}).
